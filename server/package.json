{
  "name": "ceodrior-server",
  "version": "1.0.0",
  "description": "A server app built using [Shelf](https://pub.dev/packages/shelf), configured to enable running with [Docker](https://www.docker.com/).",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "start": "nodemon --watch bin/**/*.dart --watch lib/**/*.dart --exec dart bin/server.dart",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
